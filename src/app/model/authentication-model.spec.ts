import { AuthenticationModel } from './authentication-model';

describe('AuthenticationModel', () => {
  it('should create an instance', () => {
    expect(new AuthenticationModel()).toBeTruthy();
  });
});
